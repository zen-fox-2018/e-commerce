const app = require('../app')

const chai = require('chai')
const chaiHttp = require('chai-http')

const expect = chai.expect

chai.use(chaiHttp)

describe('Cart Create', function() {
    it('Should return status code 500 if token no found', function(done) {

    })

    it('Should return error if product item not found', function(done) {

    })

    it('Should return error if product item not found', function(done) {

    })

    it('Should return error if user not found', function(done) {

    })
    
    it('Should return status code 200 if success create cart', function(done) {

    })

    it('Should return a user include his item list in shopping cart', function(done) {

    })

    it('Should return empty shopping cart', function(done) {

    })
})